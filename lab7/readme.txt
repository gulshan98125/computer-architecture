We have created the entities 
adder_subtractor,
ALU_flags,
instruction_decoder and
register_file.

by seperating them from the old design

main_processor is the top entity which contains port mappings and other functions. Its not complete yet but entities are completed.